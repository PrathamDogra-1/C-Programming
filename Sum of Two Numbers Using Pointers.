#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x,y;
    int *ptr1 = &x;
    int *ptr2 = &y;
    scanf("%d %d ",ptr1,ptr2);
    int sum = *ptr1+*ptr2;
    printf("%d",sum);
    return 0;
}
