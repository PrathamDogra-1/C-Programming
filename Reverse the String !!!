#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
    char str[1001],revstr[1001];
    scanf("%[^\n]",str);
    int k=0;
    for(int i=strlen(str)-1;i>=0;i--){
        revstr[k++] = str[i];
    }
    revstr[k]='\0';
    printf("%s",revstr);
    return 0;
}
