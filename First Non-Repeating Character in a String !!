#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main(){
    char str[1000],repeat='\0';
    fgets(str,sizeof(str),stdin);
    for(int i = 0;str[i]!='\0';i++){
        int count=0;
        for(int j=0;str[j]!='\0';j++){
            if(str[i]==str[j]){
                count++;
            }
        }
        if (count == 1) {
            repeat=str[i];
            break;
    }}
    if(repeat!='\0'){
        printf("%c",repeat);
    }
    else{
        printf("-1");
    }
    return 0;
}
