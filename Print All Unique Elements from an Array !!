#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,count=0;
    scanf("%d\n",&n);
    int list[n+1];
    for(int i=0;i<n;i++){
        scanf("%d",&list[i]); 
    }
    for(int b=0;b<n;b++){
        int unique=0;
        for(int i=0;i<n;i++){
           if(list[i]==list[b]){
            unique++;
           }
        }
        if(unique==1){
            printf("%d ",list[b]);
            count=1;
        }
    }
    if(count!=1){
        printf("No unique elements");
    }
    return 0;
}
